# CityPal server — no API keys required. Data comes from the free, keyless
# OpenStreetMap Overpass API. Copy this file to ".env" only if you want to
# change the port.

# Port the API server listens on.
PORT=8787
